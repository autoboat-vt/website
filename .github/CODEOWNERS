# Code ownership rules.
# See https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/about-code-owners
#
# `external/cicd/` is a vendored copy of VT's S4 CI templates, owned upstream
# by the s4-hosting-sites/cicd project on code.vt.edu (GitLab). The files are
# committed directly to this repo (NOT a submodule). Updates are pulled in via
# `scripts/bump-cicd.sh` — see `.github/instructions/deploy.instructions.md`.
# Changes here should be limited to upstream syncs; pair with a GitHub branch
# protection rule requiring CODEOWNERS review for the `external/` path.

# Default owner for everything not matched below.
*                           @autoboat-vt/software

# Vendored copy of VT's S4 CI templates (upstream: s4-hosting-sites/cicd on
# code.vt.edu GitLab). Updates should come from `scripts/bump-cicd.sh`, not
# hand-edits; these rules ensure any PR touching the vendored files gets
# reviewed by the software team.
external/                   @autoboat-vt/software
external/cicd/              @autoboat-vt/software
