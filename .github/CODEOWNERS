# Code ownership rules.
# See https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/about-code-owners
#
# The external/cicd submodule is read-only for humans. Any PR that touches
# the submodule pointer or its contents requires explicit review from the
# Software Officer (or the @autoboat-vt/software team). Pair this file with
# a GitHub branch protection rule that requires CODEOWNERS review for the
# `external/` path.

# Default owner for everything not matched below.
*                           @autoboat-vt/software

# Read-only submodule. Direct commits to its contents are blocked by the
# pre-commit hook and the CI read-only check; this rule ensures any PR
# bumping the pointer still gets reviewed.
external/                   @autoboat-vt/software
external/cicd/              @autoboat-vt/software
